# 가족 식단 웹 관리 규칙

- 식단을 새로 구성하거나 기존 메뉴·레시피·장보기 내용을 수정하기 전에 반드시 `MEAL.md`를 전체 읽고 기준으로 삼는다.
- 메뉴 원칙, 가족 구성, 금지 식재료, 어른·아기 분리 조리, 메뉴 중복 방지, 장보기 기준은 `MEAL.md`를 우선한다.
- 식단 관련 영구 데이터의 기준 저장소는 SQLite이며, 데이터 모델은 `prisma/schema.prisma`에서 관리한다. 화면의 임시 상수나 샘플 데이터를 실제 기록으로 취급하지 않는다.
- 변경 사항은 월간 식단·주간 식단·레시피·장보기·보유 재료 데이터에 서로 일관되게 반영한다.
- 기존 월의 기록은 삭제하거나 덮어쓰지 않는다. 월별 기록은 식단 날짜를 기준으로 SQLite에 누적 보존한다.
- 새 월 식단은 SQLite에 저장된 직전 월 식단의 주찬과 대조한다. 같은 메뉴명을 피하고 단백질·조리법·식감·메뉴 구성을 다양하게 만든다. 보유 재료는 우선 사용하되 이전 달 메뉴를 그대로 복제하지 않는다.
- 새 월 식단 확정 전 날짜순 주찬 목록을 점검한다. 같은 주찬은 월 1회, 같은 주재료·같은 조리법은 최소 3일 간격을 원칙으로 한다. 부찬 2개는 한 번 조리하면 3일 연속 먹는 묶음으로 구성한다. 동일한 부찬 2개 조합은 같은 달과 직전 월에 반복하지 않으며, 개별 부찬은 다른 조합으로 한 달에 최대 3개 묶음까지 편성할 수 있다. 새 월의 고유 부찬 중 최소 40%는 직전 월에 없던 부찬으로 구성하고, 직전 월에 2개 이상 묶음에서 사용한 부찬은 새 월에 최대 1개 묶음만 편성한다.
- 장보기 데이터는 품목마다 하나의 주차와 연결한다. 해당 주 레시피에 필요한 합산량만 표시하고 다음 주·다음 달 재료를 미리 넣지 않는다. 보유 재료를 차감한 부족분만 구매 항목으로 저장한다.
- 레시피와 주간 식단은 사용자가 선택한 월·주차 데이터만 보여준다. 장보기는 선택한 주차의 구매 항목만 표시한다.
- 식단이나 데이터 모델을 수정한 뒤 `npx prisma validate`와 `npm run build`를 실행한다. DB 구조를 바꾼 경우 마이그레이션 또는 `prisma db push` 적용 여부도 확인한다.
