# 가족 식단 웹 데이터 운영 규칙

- 식단·레시피·장보기·보유 재료를 변경하기 전에 `MEAL.md`를 전체 읽는다.
- 영구 데이터의 기준 저장소는 SQLite이며 데이터 모델은 `prisma/schema.prisma`에서 관리한다. 화면 상수나 AI 응답 자체를 저장된 기록으로 간주하지 않는다.
- AI는 먼저 대상 날짜·주차·월과 현재 데이터를 조회하고, 사용자가 요청한 범위만 변경한다. 대상이 여러 개로 해석되면 저장하지 않고 확인 질문을 한다.
- 자동 예약 생성은 기존 월을 덮어쓰지 않는다. 채팅에서 사용자가 다시 짜기·새로 구성하기·바꾸기처럼 교체 의사를 분명히 밝히면 특정 명령어를 요구하지 않고 SQLite 백업과 트랜잭션을 만든 뒤 전체 또는 지정 날짜 이후를 교체한다.
- 식단 저장, 레시피 생성, 장보기 계산은 독립된 단계로 처리한다. 레시피 검색 실패로 이미 검증된 식단 변경을 취소하지 않는다.
- 레시피가 변경되면 해당 주의 자동 장보기를 다시 계산한다. 직접 추가한 장보기 항목과 구매 완료 상태는 명시적인 삭제 요청 없이 유지한다.
- 금지 식재료, 가족 알레르기, 날짜 범위, 필수 필드, 수량, 확인하지 않은 출처는 저장 차단 오류로 취급한다. 메뉴 다양성·반복·부찬 묶음은 `MEAL.md`의 품질 목표이며 경고와 자동 보정 대상으로 취급한다.
- 삭제·월간 재설정·백업 복원처럼 영향 범위가 큰 작업은 대상을 명확히 확인한다. 모든 변경은 결과와 변경 범위를 사용자에게 알려준다.
- 데이터 모델을 수정한 뒤 `npx prisma validate`와 `npm run build`를 실행한다. DB 구조가 바뀌면 마이그레이션 또는 `prisma db push` 적용 여부도 확인한다.

- 식단 생성 전에 SQLite의 메뉴별 취향과 대상 기간의 주간 희망·기피를 조회한다. 일반 인지도·미응답·편성 횟수·레시피 존재로 취향을 추정하지 않는다.
- 메뉴 취향 변경은 메뉴·가족 범위·적용 기간을 구분한다. 지속 취향은 `DishPreference`, 이번 주만의 요청은 `WeeklyReview`에 저장하고 서로 자동 변환하지 않는다. 사용자 확인 시각과 입력 경로를 남긴다.
- 취향 저장과 기존 식단 재구성은 별도 작업이다. 취향만 저장했다면 기존 식단은 유지하고 결과와 이후 적용 범위를 알린다.
- 새로 편성하는 가족 기피(`AVOID`) 메뉴는 저장 전에 차단한다. 미확인(`UNKNOWN`) 메뉴는 저장할 수 있으나 취향을 자동으로 `ALLOW`로 바꾸지 않는다. 기존에 저장된 메뉴를 유지하거나 레시피만 보충하는 작업은 차단하지 않는다.
- 장보기 계산이 일부 누락되면 완료로 보고하지 말고 빠진 날짜·메뉴를 알린다.

- 월간 식단 생성에서는 `data/10000recipe-catalog.db`를 메뉴 후보 원본으로 조회한다. `메인반찬`, `밑반찬`, `국/탕`, `찌개`, `면/만두`, `밥/죽/떡`은 검색 분류이며 저장할 식단 역할과 혼동하지 않는다.
- 생성은 식사 형태, 조리계열, 기본 메뉴, 세부 메뉴 순서로 진행한다. 식사 형태는 `MAIN_DISH`, `SOUP_MEAL`, `NOODLE_DUMPLING`, `RICE_PORRIDGE_TTEOK` 중 하나이며 조리계열과 분리한다. `SOUP_MEAL`에는 국·탕·찌개와 간단한 주찬을 모두 저장한다.
- 카탈로그 선택은 `식사형태 > 조리계열 > 기본메뉴 > 세부메뉴` 순서다. 기본메뉴의 방법별 `cookingMethods`와 재료별 `ingredientCategories`에는 만개의레시피에서 해당 종류별 분류를 먼저 선택한 뒤 방법별·재료별 필터를 적용한 화면의 상단 기본메뉴 태그에 직접 표시된 정확한 이름만 저장한다. 단독 필터 화면의 상단 인기 태그나 개별 레시피 결과 제목으로 해당 종류별 기본메뉴 태그를 만들지 않는다. 조합 화면의 상단 태그가 없으면 `UNRESOLVED`로 남기고 식단 선택 시에만 메뉴명·원본 종류별 분류로 임시 보완한다. 기본메뉴가 여러 재료별 분류에 표시되면 첫 번째 단백질 태그를 모든 세부메뉴의 실제 주재료로 단정하지 않는다. 기본메뉴 태그는 실제 레시피 재료·함량·알레르기 판정 근거가 아니며, 세부메뉴 레시피가 연결되면 그 메뉴의 실제 재료·조리법을 우선한다. 기본메뉴마다 같은 기본 확률을 두고 세부메뉴 수로 확률을 늘리지 않는다. 최근 30일의 같은 세부메뉴는 제외 또는 매우 강한 감점, 같은 기본메뉴는 10일 강한 감점과 14일까지 완화 감점을 유지한다. 같은 유사메뉴그룹은 5일 강한 감점과 8일까지 완화 감점, 같은 조리계열·주재료는 최근 1~2일 약한 감점을 적용한다. 주찬·국·부찬의 선택 이력은 역할별로 분리한다.
- 월말 다음 달 자동 편성, 채팅 월간 생성·재생성, 주간 점검의 날짜 변경에는 저장 전 동일한 최종 식단 품질 점검을 적용한다. 정확한 카탈로그 세부메뉴 일치, 명시적으로 확인된 집 메뉴·출처가 확인된 레시피, 메뉴 역할, 추정 조리법·주재료, 주찬 유사메뉴 근접 반복과 주간 쏠림을 함께 점수와 경고로 남긴다. `UNKNOWN` 취향은 허용 추정으로 쓰지 않으며 카탈로그 밖 메뉴는 이름이 익숙해 보여도 검증된 메뉴로 간주하지 않는다. 높은 경고는 영향받은 날짜의 메뉴를 한 번 자동 보정하고 다시 검증하되 명시적 메뉴 요청과 의도한 2~3일 부찬 묶음을 보존한다. 보정 실패나 후보 부족은 저장 차단 오류로 바꾸지 않고 유효한 원본과 경고를 유지한다.
- 카탈로그에서 세부메뉴를 선택할 때는 `AVOID` 항목을 제외하고 `ALLOW`와 `UNKNOWN`을 후보로 쓴다. 기본메뉴의 허용이나 일반적인 유사성으로 세부메뉴를 허용으로 간주하지 않는다. 주간 기피, 알레르기와 금지 식재료는 후보에서 제외한다.
- 실제 레시피 사이트에서 찾은 미확인 메뉴도 식단에 넣을 수 있다. 식단 사용 사실로 취향을 자동 변경하지 않고, 가족이 평가한 경우에만 정확한 세부메뉴를 `ALLOW` 또는 `AVOID`로 기록한다.
